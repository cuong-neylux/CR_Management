<mvc:View
	controllerName="NYX.bsincrv01.ext.controller.PlanningCalendar"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns:unified="sap.ui.unified"
	xmlns="sap.m">
	<VBox id="planningCalendar-Vbox" class="sapUiSmallMargin">
		<PlanningCalendar
				id="PC1"
				startDate="{path: '/startDate'}"
				rows="{path: '/people'}"
				appointmentsVisualization="Filled">
			<toolbarContent>
				<Title id="planningCalendar-Title" text="Title" titleStyle="H4"/>
				<Label id="planningCalendar-Label" text="Logged in as" />
				<Select
						id="userRole"
						change="handleRoleChange"
						selectedKey="admin"
						width="230px">
					<items>
						<core:Item id="planningCalendar-Item1" key="admin" text="Admin" />
						<core:Item id="planningCalendar-Item2" key="manager" text="Manager" />
						<core:Item id="planningCalendar-Item3" key="donna" text="Donna Moore" />
					</items>
				</Select>
			</toolbarContent>
			<rows>
				<PlanningCalendarRow
				id="planningCalendar-Row"
						icon="{pic}"
						title="{name}"
						text="{role}"
						enableAppointmentsDragAndDrop="{path:'name', formatter:'.canModifyAppointments'}"
						enableAppointmentsResize="{path:'name', formatter:'.canModifyAppointments'}"
						enableAppointmentsCreate="{path:'name', formatter:'.canModifyAppointments'}"
						appointmentDrop="handleAppointmentDrop"
						appointmentDragEnter="handleAppointmentDragEnter"
						appointmentResize="handleAppointmentResize"
						appointmentCreate="handleAppointmentCreate"
						appointments="{path : 'appointments', templateShareable: false}">
					<appointments>
						<unified:CalendarAppointment
						id="planningCalendar-Appointment"
								startDate="{start}"
								endDate="{end}"
								icon="{pic}"
								title="{title}"
								text="{info}"
								type="{type}"
								tentative="{tentative}">
						</unified:CalendarAppointment>
					</appointments>
				</PlanningCalendarRow>
			</rows>
		</PlanningCalendar>
	</VBox>
</mvc:View>